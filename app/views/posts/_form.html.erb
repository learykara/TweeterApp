<!--Create a form for the post returned from the create query,
	send entry in text field to content.-->
<%= form_for @post do |f| %>
	<%= f.label :post %>
	<%= f.text_field :content %>
	<%= f.submit %>
<% end %>